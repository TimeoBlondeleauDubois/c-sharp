@*

    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860

*@

@{

}

@model Livre.Models.Entities.Film

<h1>Edit Films</h1>

@if (Model is null)

{

    <p>No films found, try with another ID</p>

}

else

{
    @if (User.IsInRole("Administrateur"))
    {
        <form method="post">

            <div class="mt-3">

                <label class="form-label">Id</label>

                <input type="text" class="form-control" asp-for="Id" readonly />

            </div>

            <div class="mt-3">

                <label class="form-label">Titre</label>

                <input type="text" class="form-control" asp-for="Titre" />

            </div>

            <div class="mt-3">

                <label class="form-label">Réalisateur</label>

                <input type="text" class="form-control" asp-for="Realisateur" />

            </div>

            <div class="mt-3">

                <label class="form-label">Année</label>

                <input type="text" class="form-control" asp-for="Annee" />

            </div>

            <div class="mt-3">

                <label class="form-label">Genre</label>

                <input type="text" class="form-control" asp-for="Genre" />

            </div>

            <div class="mt-3">

                <button type="submit" class="btn btn-primary" asp-controller="Film" asp-action="Edit">Update</button>

                <button type="submit" class="btn btn-danger ms-3" asp-controller="Film" asp-action="Delete"
                onclick="return confirm('Are you sure you want to delete this film?');">
                    Delete
                </button>
            </div>

        </form>
    }
    else
    {
        <p>Vous ne pouvez pas acceder a cette page.</p>
    }
}